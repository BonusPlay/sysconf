{
  programs.alacritty = {
    enable = true;
    # TODO: disable URL clicking
    settings = {
      font.normal = {
        family = "monospace";
        style = "Regular";
      };
      window.opacity = 0.9;
    };
  };
}
